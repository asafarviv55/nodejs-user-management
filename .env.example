# Application
NODE_ENV=development
PORT=3001

# Database (MySQL)
DATABASE_URL="mysql://user:password@localhost:3306/user_management"

# JWT Configuration
JWT_SECRET=your-super-secret-key-min-32-characters-long
JWT_EXPIRES_IN=10d

# CORS (optional - defaults to *)
CORS_ORIGIN=*
